# Sections

The current implementation keeps section components colocated in `src/App.tsx` to make shared state orchestration explicit. This directory is reserved for extracting Dashboard, Plan, PreTrade, TradeLog, Emotions, Goals, Stats, Journal, Notifications and NoTradePanel into standalone section modules as the app grows.
